# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.coverage
htmlcov/

# Env / secrets
.env
.env.*
!.env.example

# Databases (dev/test artifacts)
*.db
*.sqlite3
backups/

# Conteúdo dos anexos em dev (ADR 0007) — em produção é um volume do Compose
attachments_data/

# Node / frontend build artifacts
node_modules/
dist/
coverage/
playwright-report/
test-results/
*.tsbuildinfo
# Intermediário do typegen (o src/types/api.gen.ts gerado é commitado)
frontend/openapi.json
# Saída do `npm run shots` (capturas de tela do redesign)
output/

# Agent orchestration transient state
.omg/
